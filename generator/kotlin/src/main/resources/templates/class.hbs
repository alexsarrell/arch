class {{className}}({{#each properties}}
    /**
    * @since {{version}}
    */
    {{#if isOpen}}open {{/if}}val {{name}}: {{{type}}}{{#unless required}}?{{/unless}}{{#if defaultValue}} = {{{defaultValue}}}{{/if}},{{/each}}{{#each parentProperties}}
    /**
    * @since {{version}}
    */
    override val {{name}}: {{{type}}}{{#unless required}}?{{/unless}}{{#if defaultValue}} = {{{defaultValue}}}{{/if}},{{/each}}
){{#if parent}}: {{parent}}({{#each parentProperties}}
    {{name}} = {{name}},{{/each}}
){{/if}} {{#unless (isEmpty enum)}} {
    enum class {{className}}Enum { {{#each enum}}
        {{this}},{{/each}}
    }
}{{/unless}}
{{#if metadataAccessors}}{{> metadata-accessor}}{{/if}}